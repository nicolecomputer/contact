{
  "name": "contact-me",
  "version": "1.0.0",
  "main": "dist/index.js",
  "author": "Nicole Watts <76132106+nicolecomputer@users.noreply.github.com>",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "NODE_ENV=development yarn generate-schema && tsx watch src/index.ts",
    "generate-schema": "tsx scripts/generate-schema.ts",
    "db:migrate": "yarn generate-schema && prisma migrate deploy",
    "db:studio": "yarn generate-schema && prisma studio",
    "postinstall": "yarn generate-schema"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/formbody": "^8.0.1",
    "@fastify/static": "^8.0.3",
    "@prisma/client": "^5.22.0",
    "dotenv": "^16.4.5",
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.3",
    "pino-pretty": "^13.0.0",
    "prisma": "^5.22.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
